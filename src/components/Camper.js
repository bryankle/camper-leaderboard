import React, { Component } from 'react';
import ReactDOM from 'react-dom';

const Camper = ({username, alltime, recent}) => {
	console.log('username')
	return (
		<div>
			{username}
		</div>
	)
}

export default Camper;
